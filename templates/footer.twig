<footer class="footer">
  <div class="footer-container">
    &copy; {{ "now"|date("Y") }} TicketApp. All rights reserved.
  </div>

  <style>
    .footer {
      background-color: #1e3a8a; /* dark blue to match modal title */
      color: #fff;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.1); /* subtle inner shadow */
      margin-top: 3rem; /* mt-12 */
      width: 100%;
    }

    .footer-container {
      max-width: 1440px;
      margin: 0 auto;
      padding: 1.5rem 1.5rem; /* px-6 py-6 */
      text-align: center;
      font-size: 0.875rem; /* text-sm */
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
    }

    .footer a {
      color: #93c5fd; /* light blue links */
      text-decoration: none;
      margin: 0 0.5rem;
    }

    .footer a:hover {
      text-decoration: underline;
    }
  </style>
</footer>
