<footer class="footer">
  <div class="footer-container">
    &copy; {{ "now"|date("Y") }} TicketApp. All rights reserved.
  </div>

  <style>
    .footer {
      background-color: #ffffff; /* bg-white */
      box-shadow: inset 0 1px 0 rgba(0, 0, 0, 0.05); /* shadow-inner */
      margin-top: 3rem; /* mt-12 */
      width: 100%;
    }

    .footer-container {
      max-width: 1440px;
      margin-left: auto;
      margin-right: auto;
      padding: 1.5rem 1.5rem; /* px-6 py-6 */
      text-align: center;
      color: #4b5563; /* text-gray-600 */
      font-size: 0.875rem; /* text-sm */
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
    }
  </style>
</footer>
